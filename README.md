# 🛍 ShopProject1

ShopProject1 — учебный Python-проект магазина, демонстрирующий работу с объектной моделью товаров, наследование классов, перегрузку операторов, итерацию по товарам категории, загрузку данных из JSON и тестирование с помощью pytest.

## 📌 Оглавление

💡 Обзор

🚀 Быстрый старт

📁 Структура проекта

📦 Установка

🧠 Использование

📊 Формат JSON

🧪 Тестирование

📋 Классы

➕ Операции с товарами

🔁 Итерация по товарам

🧩 Авторы и лицензия

## 💡 Обзор

Проект реализует:

- базовый класс Product для всех товаров;

- категории товаров (Category) с защитой от добавления некорректных объектов;

- наследников товаров:

    - Smartphone — смартфоны;

    - LawnGrass — газонная трава;

- перегрузку операторов (__add__) для подсчёта общей стоимости товаров;

- строковое представление (__str__) для товаров и категорий;

- итерацию по товарам категории через отдельный итератор;

- загрузку данных из JSON;

- автоматические тесты на pytest.

- абстрактный базовый класс BaseProduct для унификации товаров;

- миксин InitLoggerMixin для логирования создания объектов;

- контроль изменения цены через setter с подтверждением пользователя;

- фабричный метод Product.new_product для создания и объединения товаров;

- отдельный класс CategoryIterator для итерации по товарам категории.

## 🚀 Быстрый старт

Клонируйте репозиторий и перейдите в папку проекта:
~~~
git clone https://github.com/mrtimpr/ShopProject1.git
cd ShopProject1
~~~

## 📁 Структура проекта
ShopProject1/
├── data/
│   └── products.json
├── src/
│   ├── models/
│   │   ├── product.py
│   │   ├── category.py
│   │   └── __init__.py
├── tests/
│   └── test_models.py
├── main.py
├── requirements.txt
└── README.md

## 📦 Установка

Создайте виртуальное окружение (рекомендуется):
~~~
python3 -m venv venv
source venv/bin/activate   # Linux/macOS
venv\Scripts\activate      # Windows
~~~

Установите зависимости:
~~~
pip install -r requirements.txt
~~~
## 🧠 Использование

Запуск демонстрационного примера:
~~~
python main.py
~~~

Пример загрузки данных из JSON:
~~~
from src.utils import load_categories_from_json

categories = load_categories_from_json("data/products.json")

for category in categories:
    print(category)
~~~
## 📊 Формат JSON

Файл data/products.json должен иметь формат:
~~~
[
  {
    "name": "Смартфоны",
    "description": "Описание категории",
    "products": [
      {
        "name": "Название товара",
        "description": "Описание товара",
        "price": 123.45,
        "quantity": 10
      }
    ]
  }
]
~~~
## 🧪 Тестирование

Проект покрыт тестами с использованием pytest.

Запуск тестов:
~~~
pytest
~~~

Тесты проверяют:

- корректность инициализации всех классов;

- работу счётчиков категорий и товаров;

- корректную работу __add__;

- запрет сложения товаров разных типов;

- защиту метода add_product() от некорректных объектов;

- итерацию по товарам категории;

- загрузку данных из JSON.

## 📋 Классы
### 🧩 BaseProduct (абстрактный класс)

Абстрактный базовый класс для всех товаров.

Определяет обязательный интерфейс:
- свойство price с геттером и сеттером;
- методы __str__ и __add__.

Гарантирует единое поведение всех товаров и их наследников.

### 📦 Product

Базовый класс для всех товаров.

#### InitLoggerMixin

Миксин, используемый в классе Product.

При создании объекта выводит его строковое представление,
что упрощает отладку и отслеживание жизненного цикла объектов.

### Атрибуты:

- name: str — название

- description: str — описание

- price: float — цена

- quantity: int — количество

### Возможности:

- строковое представление:
~~~
Название продукта, 80 руб. Остаток: 15 шт.
~~~

поддержка сложения товаров одного типа (__add__).

- контроль изменения цены:
  - запрещена нулевая и отрицательная цена;
  - при понижении цены требуется подтверждение пользователя;

- фабричный метод new_product:
  - создаёт новый товар;
  - либо объединяет его с существующим товаром по имени,
    увеличивая количество и обновляя цену при необходимости.

## 📱 Smartphone (наследник Product)

Дополнительные атрибуты:

- efficiency: float — производительность

- model: str — модель

- memory: int — объём памяти

- color: str — цвет

## 🌱 LawnGrass (наследник Product)

Дополнительные атрибуты:

- country: str — страна-производитель

- germination_period: int — срок прорастания (в днях)

- color: str — цвет

## 🗂 Category

Представляет категорию товаров.

### Атрибуты:

- name: str — название

- description: str — описание

- _products: List[Product] — список товаров (приватный)

### Свойства и методы:

- products — геттер для вывода товаров в строковом виде;

- add_product(product) — добавление товара (только Product и наследники);

- __str__ — строковое представление:
~~~
Название категории, количество продуктов: 200 шт.
~~~

### Счётчики класса:

- Category.category_count — количество категорий

- Category.product_count — общее количество товаров

## ➕ Операции с товарами

Товары одного типа можно складывать:
~~~
result = smartphone1 + smartphone2
~~~

Результат — общая стоимость всех товаров на складе:
~~~
price * quantity + price * quantity
~~~

❌ Сложение товаров разных классов запрещено:
~~~
smartphone1 + grass1  # TypeError
~~~
## 🔁 Итерация по товарам

Реализован вспомогательный класс-итератор для категории:
~~~
for product in category:
    print(product)
~~~

Методы:

- `__iter__`

- `__next__`

Позволяют последовательно перебирать товары категории.

Итерация реализована через отдельный класс CategoryIterator,
который работает с внутренним списком товаров категории.

## 🧩 Авторы и лицензия

Проект разработан в учебных целях.
Лицензия: MIT.